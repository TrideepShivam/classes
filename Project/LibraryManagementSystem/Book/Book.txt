1. Structure is Book.
2. void B_Register ( Book B_R);
    Its take "Book" type values 
    and sent every data into "Book.dat" file.
    Also print the values inserted successfully into that file or not. 
3. Book B_Retrive (int id);
    this will recieve the id of the book and return all data of book 
    after searching from the book.dat file if available
    otherwise will return null.
4. get_book_by_author(const char *author);
    this will recieve the auther name of the book and print all data of book 
    after searching from the book.dat file if available
    otherwise will print "Sorry, Not found".
5. get_ID();
    This will find the last Book_ID from Book.dat and 
    return the Book_ID after adding 1 .  
6. void get_book_by_genere(const char *genere);
    this will recieve the genere of the book and print all data of book 
    after searching from the book.dat file if available
    otherwise will print "Sorry, Not found".
7. get_book_by_name(const char *name) ;    
    this will recieve the name of the book and print all data of book 
    after searching from the book.dat file if available
    otherwise will print "Sorry, Not found".
8. update_book_count(int ID ,int count);
    update_book_count
    Updates the book quantity in Book.dat by matching Book_ID 
    and rewriting the record.
9. Showall_Book ();
    this fuction will  print all book details ..
